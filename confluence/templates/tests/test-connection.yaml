apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "confluence.fullname" . }}-test-connection"
  labels:
    {{- include "confluence.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "confluence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
